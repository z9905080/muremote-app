{
  "name": "muremote-pc",
  "version": "1.0.0",
  "description": "MuRemote PC Client - Remote control for MuMu Emulator",
  "main": "src/main/main.js",
  "scripts": {
    "start": "electron .",
    "postinstall": "node scripts/download-adb.js && node scripts/download-scrcpy.js",
    "prebuild": "node scripts/download-adb.js && node scripts/download-scrcpy.js",
    "build": "npm run prebuild && electron-builder --win"
  },
  "author": "MuRemote Team",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-builder": "24.6.3",
    "extract-zip": "^2.0.1"
  },
  "dependencies": {
    "adbkit": "^2.11.0",
    "adbkit-logcat": "^1.1.0",
    "adbkit-monkey": "~1.0.1",
    "bonjour": "^3.5.1",
    "electron-log": "^5.0.0",
    "iconv-lite": "^0.6.3",
    "qrcode": "^1.5.4",
    "uuid": "^9.0.0",
    "ws": "^8.14.0"
  },
  "build": {
    "appId": "com.muremote.app",
    "productName": "MuRemote PC",
    "extraResources": [
      {
        "from": "resources/adb/",
        "to": "adb",
        "filter": [
          "**/*"
        ]
      },
      {
        "from": "resources/scrcpy/",
        "to": "scrcpy",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "target": "nsis"
    }
  }
}
